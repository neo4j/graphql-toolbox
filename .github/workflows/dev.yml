name: "dev"

concurrency: "dev"

on:
  push:
    branches:
      - dev

jobs:
  code-scanning:
    uses: ./.github/workflows/reusable-codeql-analysis.yml

  toolbox-tests:
    uses: ./.github/workflows/reusable-toolbox-tests.yml

  toolbox-deploy:
    uses: ./.github/workflows/toolbox-deploy.yml
    needs: 
      - toolbox-tests
